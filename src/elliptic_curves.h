#include "config_pc.hpp"

void get_aux_data(vector<F> &data);
vector<vector<F>> compute_exponent(F x, F y, F z, F scalar);
vector<F> get_aggr_aux();
F add_points(F x1,F x2, F y1, F y2);
void elliptic_curves_init();